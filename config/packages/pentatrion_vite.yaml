# Vite integration for the SolidJS frontend (frontend/).
# Vite builds into public/build-ui; public/build only holds committed static
# assets (logo images) left over from the removed Encore/ExtJS build.
pentatrion_vite:
    public_directory: public
    build_directory: build-ui
