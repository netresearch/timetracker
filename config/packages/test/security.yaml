security:
    firewalls:
        main:
            # Disable the firewall in test to avoid redirects; controllers handle auth checks
            security: false
            remember_me: false

framework:
    router:
        resource: '%kernel.project_dir%/config/routes.yaml'

# Ensure logout route is available in tests (route exists in routes.yaml). No further changes needed.

