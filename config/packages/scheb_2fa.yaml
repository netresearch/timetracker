# Two-factor authentication (ADR-018 D2). TOTP + backup codes.
# Reference: https://symfony.com/bundles/SchebTwoFactorBundle/6.x/configuration.html
#
# The `main` firewall enables `two_factor` (see security.yaml): users with an
# enrolled TOTP secret are challenged after the password step; everyone else
# logs in unchanged.
scheb_two_factor:
    security_tokens:
        - Symfony\Component\Security\Core\Authentication\Token\UsernamePasswordToken
        - Symfony\Component\Security\Http\Authenticator\Token\PostAuthenticationToken

    # RFC-6238 TOTP, compatible with Google Authenticator, Aegis, 1Password, etc.
    # The issuer/label is what shows in the authenticator app. The per-code
    # parameters (digits/period/algorithm) are supplied per user by
    # User::getTotpAuthenticationConfiguration().
    totp:
        enabled: true
        issuer: '%app_title%'
        # Accept a code from the previous window for this many seconds — absorbs
        # small device-clock skew and a period tick between typing and verifying
        # (enrolment already allows ±1 period; this aligns the login check).
        leeway: 15

    # One-time recovery codes for when the authenticator device is unavailable.
    backup_codes:
        enabled: true
