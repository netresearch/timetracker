name: Auto-merge dependency PRs

on:
  pull_request:

permissions: {}

jobs:
  auto-merge:
    permissions:
      contents: write
      pull-requests: write
    uses: netresearch/.github/.github/workflows/auto-merge-deps.yml@main
