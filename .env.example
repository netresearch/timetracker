# Docker Compose Profile Configuration
# Set to dev, prod, or test
COMPOSE_PROFILES=dev

# Application environment
APP_ENV=dev
APP_DEBUG=1

# HTTP port for web server
HTTP_PORT=8765

# Database configuration (main database)
DB_ROOT_PASSWORD=global123
DB_USER=timetracker
DB_PASSWORD=timetracker
DB_NAME=timetracker

# Test database configuration  
TEST_DB_ROOT_PASSWORD=global123
TEST_DB_USER=unittest
TEST_DB_PASSWORD=unittest
TEST_DB_NAME=unittest

# Proxy configuration
TRUSTED_PROXY_ALL=
TRUSTED_PROXY_LIST=